{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-owner",
  "description": "Minimal capabilities for the packaged local-owner control plane.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled",
    "process:allow-restart",
    "updater:default"
  ]
}
